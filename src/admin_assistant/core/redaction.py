from __future__ import annotations


def redact_sensitive_text(raw_text: str) -> str:
    """Placeholder for future secret redaction logic."""
    return raw_text


def truncate_for_ai(raw_text: str, max_chars: int = 8_000) -> str:
    """Placeholder for future token-aware truncation logic."""
    return raw_text[:max_chars]

